###########################################################
# auto generated by project                               #
# do not reference directly using id.                     #
# you shall always reference with:                        #
#  - ${local.xxx_prefix}                                  #
#  - ${terraform.workspace}                               #
#  - ${local.route53_zone_prefix}                         #
###########################################################
data "aws_lb" "ezdmzalb_pte" {
  tags = {
    "Project-Code" = "sharedinfra"
    "Environment" = "${terraform.workspace}"
    "Terraform" = "true"
    "Tier" = "dmz"
    "Zone" = "ez"
    "type" = "${local.alb_prefix}"
    "scheme" = "private"
  }
}

data "aws_lb_listener" "ezdmzalb_pte_443" {
  load_balancer_arn = data.aws_lb.ezdmzalb_pte.arn
  port              = 443
}

data "aws_security_group" "ezdmzalb_sso" {
  filter {
    name     = "tag:Terraform"
    values = ["true"]
  }
  filter {
    name     = "tag:Environment"
    values = ["${terraform.workspace}"]
  }
  filter {
    name     = "tag:Project-Code"
    values = ["sharedinfra"]
  }
  filter {
    name     = "tag:Project-Desc"
    values = ["dmz-sso"]
  }
  filter {
    name     = "tag:Zone"
    values = ["ez"]
  }
  filter {
    name     = "tag:Tier"
    values = ["dmz"]
  }
}
