###########################################################
# auto generated by project                               #
# do not reference directly using id.                     #
# you shall always reference with:                        #
#  - ${local.xxx_prefix}                                  #
#  - ${terraform.workspace}                               #
#  - ${local.route53_zone_prefix}                         #
###########################################################
################################################################################
# # nacl is different from subnets route_tables and security_groups
# # nacl does not have a data.aws_network_acl
# # it only has nacl.aws_network_acls (note the S) which only return a list of nacl_ids
# # the typical auto gen with vpc_id will not work.
################################################################################
data "aws_network_acls" "sharedinfra_ez_all" {
  filter {
    name   = "tag:Terraform"
    values = ["true"]
  }
  filter {
    name   = "tag:Environment"
    values = ["${terraform.workspace}"]
  }
  filter {
    name   = "tag:Project-Code"
    values = ["sharedinfra"]
  }
  filter {
    name   = "tag:Zone"
    values = ["ez"]
  }
}

data "aws_network_acls" "sharedinfra_ez_dmz" {
  filter {
    name   = "tag:Terraform"
    values = ["true"]
  }
  filter {
    name   = "tag:Environment"
    values = ["${terraform.workspace}"]
  }
  filter {
    name   = "tag:Project-Code"
    values = ["sharedinfra"]
  }
  filter {
    name   = "tag:Zone"
    values = ["ez"]
  }
  filter {
    name   = "tag:Tier"
    values = ["dmz"]
  }
}

data "aws_network_acls" "sharedinfra_ez_egress" {
  filter {
    name   = "tag:Terraform"
    values = ["true"]
  }
  filter {
    name   = "tag:Environment"
    values = ["${terraform.workspace}"]
  }
  filter {
    name   = "tag:Project-Code"
    values = ["sharedinfra"]
  }
  filter {
    name   = "tag:Zone"
    values = ["ez"]
  }
  filter {
    name   = "tag:Tier"
    values = ["egress"]
  }
}

data "aws_network_acls" "sharedinfra_ez_endpt" {
  filter {
    name   = "tag:Terraform"
    values = ["true"]
  }
  filter {
    name   = "tag:Environment"
    values = ["${terraform.workspace}"]
  }
  filter {
    name   = "tag:Project-Code"
    values = ["sharedinfra"]
  }
  filter {
    name   = "tag:Zone"
    values = ["ez"]
  }
  filter {
    name   = "tag:Tier"
    values = ["endpt"]
  }
}

data "aws_network_acls" "sharedinfra_ez_ingress" {
  filter {
    name   = "tag:Terraform"
    values = ["true"]
  }
  filter {
    name   = "tag:Environment"
    values = ["${terraform.workspace}"]
  }
  filter {
    name   = "tag:Project-Code"
    values = ["sharedinfra"]
  }
  filter {
    name   = "tag:Zone"
    values = ["ez"]
  }
  filter {
    name   = "tag:Tier"
    values = ["ingress"]
  }
}

data "aws_network_acls" "sharedinfra_ez_inspect" {
  filter {
    name   = "tag:Terraform"
    values = ["true"]
  }
  filter {
    name   = "tag:Environment"
    values = ["${terraform.workspace}"]
  }
  filter {
    name   = "tag:Project-Code"
    values = ["sharedinfra"]
  }
  filter {
    name   = "tag:Zone"
    values = ["ez"]
  }
  filter {
    name   = "tag:Tier"
    values = ["inspect"]
  }
}

data "aws_network_acls" "sharedinfra_ez_tgw" {
  filter {
    name   = "tag:Terraform"
    values = ["true"]
  }
  filter {
    name   = "tag:Environment"
    values = ["${terraform.workspace}"]
  }
  filter {
    name   = "tag:Project-Code"
    values = ["sharedinfra"]
  }
  filter {
    name   = "tag:Zone"
    values = ["ez"]
  }
  filter {
    name   = "tag:Tier"
    values = ["tgw"]
  }
}

